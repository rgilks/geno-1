{
  "name": "geno-1",
  "version": "1.0.0",
  "description": "Web (WASM): uses `wasm-pack` to build and a static `index.html` to run",
  "main": "index.js",
  "scripts": {
    "test": "npm run test:web",
    "build:web": "wasm-pack build --target web --out-dir pkg --out-name app_web --release && cp env.js pkg/env.js && rm -rf dist && mkdir -p dist/pkg && cp pkg/app_web.js dist/pkg/ && cp pkg/app_web_bg.wasm dist/pkg/ && cp env.js dist/pkg/env.js && cp index.html dist/index.html",
    "build:web:dev": "wasm-pack build --target web --out-dir pkg --out-name app_web && cp env.js pkg/env.js",
    "serve:web": "http-server -p 8080 -c-1 .",
    "dev:web": "npm run build:web && npm run serve:web",
    "open:web": "open http://localhost:8080",
    "start:web": "node server.js",
    "test:web": "node web-test.js",
    "ci:web": "npm run build:web && node server.js & SERVER_PID=$!; node scripts/wait-for-server.js && node web-test.js; kill $SERVER_PID 2>/dev/null || true",
    "build:all": "npm run build:web",
    "clean": "cargo clean && rm -rf pkg",
    "check:rust": "cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace --all-targets",
    "check": "npm run check:rust && npm run ci:web"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rgilks/geno-1.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/rgilks/geno-1/issues"
  },
  "homepage": "https://github.com/rgilks/geno-1#readme",
  "devDependencies": {
    "http-server": "^14.1.1",
    "puppeteer": "^24.16.0",
    "wasm-pack": "^0.13.1"
  }
}
