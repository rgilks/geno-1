#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "[husky] Running npm run check (fmt, clippy, core tests, web build + headless test)"

npm run check || {
  echo "[husky] check failed; aborting commit"
  exit 1
}

echo "[husky] OK"


